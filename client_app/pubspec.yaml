name: cloud_zen_client
description: Cloud-Zen consent-based backup client.
publish_to: "none"

version: 1.1.0+2

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  mobile_scanner: ^6.0.2
  device_info_plus: ^11.2.0
  file_picker: ^10.3.10
  crypto: ^3.0.6
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
